<?php
 load_gc();
 $cart=find("Cart", "HC", auv("User"));
 if (is_null($cart))$cart=find("Cart", "HC", auv("cart"));
 if (is_null($cart))$cart=find("Cart", "HC", nq($_VAR['id']));
 if (is_null($cart))$cart=find("Cart", "HC", nq($uses['cart']));
 if (is_null($cart)){
  echo milford()."[[Cart]]: No Cart Found!";
  die;	
 }

 //add security here
 displayCart($cart['HC']);
 var_dump($uses);
 //Widget that displays Cart in some default way or user configurable
 unload_gc();
?>
